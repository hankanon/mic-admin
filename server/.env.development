MYSQL_USER=root
MYSQL_HOST=127.0.0.1
MYSQL_PORT=3306
MYSQL_PASS=QingDong123,.
MYSQL_DB=admin

# JWT 认证（真实 JWT：access 15min / refresh 7d）
# 生产环境必须用 `openssl rand -base64 48` 生成强密钥并通过环境注入，勿提交仓库
JWT_SECRET=dev-only-secret-change-me-0123456789abcdef0123456789abcdef
JWT_ISSUER=mic-admin
JWT_AUDIENCE=mic-admin-web
JWT_ACCESS_TTL=900
JWT_REFRESH_TTL=604800
